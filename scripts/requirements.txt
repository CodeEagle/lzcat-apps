# Python dependencies for the lzcat-migration-runner image and the
# auto-migration scripts. Keep this short and deterministic.
#
# Stdlib-only modules are not listed.

# YAML manifests / build configs
PyYAML>=6.0,<7.0

# HTTP for scout / discord / dashboard / lazycat appstore
httpx>=0.27,<1.0
requests>=2.32,<3.0

# Used by capture_web_screenshot / web_probe / functional_checker
playwright>=1.50,<2.0

# Test runner (image is also used to run tests in CI)
pytest>=8.0,<10.0
