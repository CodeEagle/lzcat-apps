name: Update Image

on:
  workflow_call:
    inputs:
      artifact_repo:
        required: true
        type: string
      config_file:
        required: true
        type: string
      target_version:
        required: false
        type: string
        default: ""
      force_build:
        required: false
        type: boolean
        default: false
      publish_to_store:
        required: false
        type: boolean
        default: false
      check_only:
        required: false
        type: boolean
        default: false

jobs:
  update-image:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from update-image"
