--- a/packages/dashboard-web/src/components/ApiKeysTab.tsx
+++ b/packages/dashboard-web/src/components/ApiKeysTab.tsx
@@ -136,2 +136,4 @@
-			// Don't invalidate queries here - wait until user authenticates
-			// This prevents 401 errors if the stored key is invalid
+			// When the first key is created, the dashboard becomes protected
+			// immediately. Reuse the freshly generated key for the current session
+			// so a page refresh does not lock the user out.
+			api.setApiKey(data.apiKey);
@@ -147,2 +149,2 @@
-		// Trigger auth dialog so user can paste the key they just copied
-		window.dispatchEvent(new CustomEvent("auth-required"));
+		queryClient.invalidateQueries({ queryKey: ["api-keys"] });
+		queryClient.invalidateQueries({ queryKey: ["api-keys-stats"] });
@@ -536,2 +538,2 @@
-						// Trigger auth dialog when closing so user can authenticate
-						window.dispatchEvent(new CustomEvent("auth-required"));
+						queryClient.invalidateQueries({ queryKey: ["api-keys"] });
+						queryClient.invalidateQueries({ queryKey: ["api-keys-stats"] });
